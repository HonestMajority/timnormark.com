apiVersion: v1
kind: Service
metadata:
  name: {{ include "timnormark-com.fullname" . }}
  labels:
    {{- include "timnormark-com.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "timnormark-com.selectorLabels" . | nindent 4 }}
